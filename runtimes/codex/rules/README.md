# Codex Rules

`default.rules.snapshot` is an exact, workstation-specific snapshot of the
current Codex approval rules. It contains absolute paths and project-specific
allow rules, so bootstrap does not apply it automatically. Review and rewrite
rules for the destination machine before use.
