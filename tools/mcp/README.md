# MCP and Tool Definitions

The active OpenCode MCP definitions are in
`opencode/config/opencode.json.template`. The active Mcporter definition is
`mcporter.json`, containing only the public Exa MCP endpoint. Credentials are
provided by environment or the selected client and are not stored here.
